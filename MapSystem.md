# 地图系统需求

## 地图系统 (Map System)

### 代码功能需求
- **坐标系**：使用 **四边形网格**。
- **寻路算法**：统一使用 `AStarPathFinder` 工具类，调用示例：`PathFinder.findPath(start, end, entity.getMoveType())`。
- **视野计算**：大地图视野更新**必须**采用**增量更新**策略，严禁全量推送视野内所有格子属性。

### 业务功能需求
- **地图资源**：地图上地格会有不同的资源类型（如森林、矿山）。
- **地图建筑**：地图上会有不同的建筑类型（如城池、哨塔），建筑会占用一个或多个格子。